// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.mstc.airticket"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
